export const USER_ID_REPLACE = '{user_id}';
export const STOCK_REPLACE = '{stock_id}';

export const enum API_PATHS {
  /// USERS
  USERS_GET = 'users',
  USERS_POST = 'users',
  USERS_PUT = 'users',

  /// FINANCIAL
  STOCKS_GET = 'stocks',
  TOKEN_REFRESH_POST = 'users/refresh-token',
}
